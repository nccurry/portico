# Copy this file to config/local.toml. The copied file is ignored by Git.
# Add only the values that differ from config/defaults.toml.

# Common examples:
# [reporting]
# lookback_months = [3, 6, 12, 24]
# default_lookback_months = 12
#
# [spending]
# default_view = "discretionary"
# exclude_categories = ["Christmas", "Given Gift", "Tax Return Payment"]
# exclude_groups = ["Bills", "Income", "Donations", "Maintenance", "Travel"]
#
# [income_savings]
# default_view = "regular"
# target_rate = 20
# exclude_categories = []
# exclude_groups = []
#
# [subscriptions]
# known_category_terms = ["subscription"]
# minimum_confidence = 80
# stale_after_days = 45
# default_exclude_categories = []
# detection_excluded_categories = []
# detection_excluded_pattern = "Mortgage|Loan|Investment|401k|HSA|RSU|ESPP"
#
# [year_over_year]
# utility_group_terms = ["bill"]
# utility_category_terms = ["electric", "gas", "water", "internet", "phone"]
#
# [budget]
# history_months = 12
#
# [data_health]
# stale_account_days = 7
# duplicate_require_same_account = true
# duplicate_require_same_category = false
# duplicate_require_same_description = true
#
# [weekly_summary]
# average_weeks = 8
# rolling_weeks = 4
# top_merchant_count = 3

# [financial_independence]
# Match account names by case-insensitive substring.
# included_account_patterns = ["Example brokerage", "Example HSA"]
# included_groups = ["Savings", "Investments", "Retirement"]
# expected_return_rate = 7.0
# withdrawal_rate = 4.0
# spending_lookback_months = 12
# projection_years = 50

# [merchants.aliases]
# Each key is the display name. Each value contains description fragments.
# "Example merchant" = ["EXAMPLE MERCHANT", "EXAMPLE MKTPLACE"]
