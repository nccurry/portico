[pytest]
testpaths = tests
pythonpath = .
cache_dir = .local/pytest-cache
addopts = --basetemp=.local/pytest-basetemp
markers =
    uses_real_dates: pin time to the configured demo reference date for date-sensitive tests
